{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimum the shell needs: the core defaults; the two window commands that the native drag region in the application header calls; `set-effects`, which both applies and \u2014 called with no effects \u2014 withdraws the window material when the system asks for reduced transparency; `show` with `set-focus`, because the window is created hidden and is revealed only once it has something to show; `dialog:allow-open`, which is how a person chooses the folder to open as a project, the files a record's path field names, and the files attached to a message in Chat \u2014 the panel answers with a path, and nothing here can read one; `dialog:allow-save`, which is the same door the other way, for saving a picture a conversation is holding \u2014 it too answers with a path and writes nothing itself, and the bytes are written in Rust by `session_image_save` from the session that already holds them; and `open-url` under the opener's own default scope, which is how a link to the web reaches the browser. Nothing else is granted \u2014 no filesystem, shell, process or network access, and none of the updater's commands although the plugin is loaded: updating is a Rust flow in `src/updates.rs` that the webview has no route into, none of the dialog plugin's other commands, and neither of the opener's others: `open-path` would launch a file on the disk and `reveal-item-in-dir` would open Finder, and a record's body is not allowed to ask for either. Autostart's three commands are granted so the Server section can offer opening Sync at login \u2014 the server is what agents reach, and an agent working while Sync is closed reaches nothing. Two labels rather than one: a person can have any number of project windows open, and the ones after the first are `main-2`, `main-3` and so on \u2014 a capability is granted per label, so a window built under a name this pattern did not cover would be one whose webview could not so much as show itself.",
  "windows": [
    "main",
    "main-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-set-effects",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:allow-open-url",
    "opener:allow-default-urls",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
