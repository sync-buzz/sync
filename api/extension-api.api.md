## API Report File for "sync"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ClassProp } from 'class-variance-authority/types';
import { ClassValue } from 'clsx';
import type { ComponentType } from 'react';
import { Dialog } from 'radix-ui';
import { DropdownMenu as DropdownMenu_2 } from 'radix-ui';
import { JSX } from 'react';
import { LucideIcon } from 'lucide-react';
import { Provider } from 'react';
import * as React_2 from 'react';
import { ReactNode } from 'react';
import { ScrollArea as ScrollArea_2 } from 'radix-ui';
import { Tooltip as Tooltip_2 } from 'radix-ui';
import { VariantProps } from 'class-variance-authority';

// @public
export function absenceLabel(presence: Presence): string;

// @public
export type ActivationResult = Readonly<Record<string, AreaModule>>;

// @public
export interface AdapterState {
    // (undocumented)
    readonly agentId: string;
    // (undocumented)
    readonly package: string;
    // (undocumented)
    readonly ready: boolean;
    // (undocumented)
    readonly version: string;
}

// @public
export function adoptWorktree(args: {
    project: string;
    path: string;
    branch: string;
}): Promise<void>;

// @public
export interface Agent extends AgentDescriptor {
    // (undocumented)
    readonly adapterReady: boolean | null;
}

// @public
export interface AgentDescriptor {
    readonly available: boolean;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly name: string;
    readonly takesModelAtLaunch: boolean;
    readonly transport: "native" | "adapter" | "bridge" | "unknown";
    readonly unavailableReason: string | null;
    // (undocumented)
    readonly unverifiedReason: string | null;
    readonly verified: boolean;
}

// @public
export interface AgentSession {
    readonly answer: (optionId: string | null) => Promise<void>;
    // (undocumented)
    readonly cancel: () => Promise<void>;
    // (undocumented)
    readonly choose: (configId: string, valueId: string) => Promise<void>;
    readonly configuration: readonly SessionConfigOption[];
    readonly isWorking: boolean;
    // (undocumented)
    readonly key: string | null;
    readonly modes: readonly SessionMode[];
    readonly prompt: (text: string, attachments?: readonly string[], images?: readonly PastedContent[]) => Promise<void>;
    readonly setMode: (modeId: string) => Promise<void>;
    // (undocumented)
    readonly transcript: Transcript;
}

// @public
export interface ApiRequirement {
    readonly capabilities?: readonly string[];
    readonly syncApi: string;
}

// @public
export type AreaIntent =
/**
* Open a record. The kind travels with the key because an area lists records
* by type: without it, an area would have to read the record to find out
* which of its own lists the row it is about to open belongs in.
*/
    {
    readonly show: "record";
    readonly key: string;
    readonly kind: string;
}
/** Show one entry of the catalogue, by extension id. */
| {
    readonly show: "extension";
    readonly id: string;
};

// @public
export interface AreaModule {
    readonly Inspector?: ComponentType;
    readonly Navigator?: ComponentType;
    // (undocumented)
    readonly Provider?: ComponentType<AreaProviderProps>;
    // (undocumented)
    readonly Workspace: ComponentType;
}

// @public
export interface AreaProviderProps {
    readonly active: boolean;
    // (undocumented)
    readonly children: ReactNode;
    readonly intent?: AreaIntent | null;
    // (undocumented)
    readonly project: OpenProject;
}

// @public
export const ATTENTION_STATES: readonly ["stale", "invalid"];

// @public
export type BadgeReport = number | "some" | null;

// @public (undocumented)
export function Button(input: React_2.ComponentProps<"button"> & VariantProps<typeof buttonVariants> & {
    asChild?: boolean;
}): React_2.JSX.Element;

// @public (undocumented)
export const buttonVariants: (props?: ({
    variant?: "link" | "default" | "outline" | "secondary" | "ghost" | "destructive" | null | undefined;
    size?: "icon" | "default" | "xs" | "sm" | "lg" | "icon-xs" | "icon-sm" | "icon-lg" | null | undefined;
} & ClassProp) | undefined) => string;

// @public
export function chooseAttachments(defaultPath: string): Promise<readonly string[]>;

// @public (undocumented)
export function cn(...inputs: ClassValue[]): string;

// @public
export function ContextInspector(input: {
    corpus: Corpus;
    open: OpenDocument | null;
    projectPath: string;
}): JSX.Element;

// @public
export function conversationForRecord(project: string, recordKey: string): Promise<RememberedConversation | null>;

// @public
export function conversationKeptAs(key: string, recordKey: string): Promise<boolean>;

// @public (undocumented)
export interface Corpus {
    readonly countRecords: (kind: string) => Promise<number>;
    readonly counts: MemoryCounts;
    readonly createRecord: (kind: string, folder?: string) => Promise<MemoryDocument>;
    readonly createType: (type: TypeDefinition) => Promise<void>;
    readonly deleteRecords: (keys: readonly string[]) => Promise<void>;
    readonly deleteType: (kind: string) => Promise<number>;
    readonly dependentsOf: (key: string) => Promise<Dependents>;
    readonly error: string | null;
    readonly hasMore: boolean;
    readonly hidden: readonly string[];
    readonly isLoading: boolean;
    readonly records: readonly MemoryRecord[];
    // (undocumented)
    readonly reload: () => void;
    readonly resolveUnmatched: (file: ScanChange, kind: string, adopt?: string) => Promise<void>;
    readonly revision: string | null;
    readonly types: readonly MemoryType[];
    readonly unmatched: readonly ScanChange[];
    readonly updateType: (type: TypeDefinition) => Promise<void>;
}

// @public
export function createMemoryFolder(project: string, folder: string, kind: string): Promise<TransactionResult>;

// @public
export function deleteMemoryFolder(project: string, folder: string): Promise<number>;

// @public
export function deleteSession(key: string): Promise<void>;

// @public
export interface Dependent {
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly kind: string;
    readonly relation: string | null;
    // (undocumented)
    readonly title: string;
}

// @public
export interface Dependents {
    // (undocumented)
    readonly links: readonly Dependent[];
    // (undocumented)
    readonly mentions: readonly Dependent[];
}

// @public
export function describeMemoryFolder(project: string, folder: string, kind: string): Promise<MemoryDocument>;

// @public
export function discardWorktree(args: {
    project: string;
    path: string;
}): Promise<void>;

// @public
export interface DocumentDraft {
    // (undocumented)
    readonly archived: boolean;
    // (undocumented)
    readonly content: string;
    // (undocumented)
    readonly fields: Readonly<Record<string, unknown>>;
    // (undocumented)
    readonly links: readonly EntityLink[];
    // (undocumented)
    readonly observed: readonly string[];
    // (undocumented)
    readonly scope: readonly string[];
    // (undocumented)
    readonly tags: readonly string[];
    // (undocumented)
    readonly title: string;
}

// @public
export interface DocumentPatch {
    // (undocumented)
    readonly archived?: boolean;
    // (undocumented)
    readonly content?: string;
    // (undocumented)
    readonly fields?: Readonly<Record<string, unknown>>;
    // (undocumented)
    readonly links?: readonly EntityLink[];
    // (undocumented)
    readonly observed?: readonly string[];
    // (undocumented)
    readonly scope?: readonly string[];
    // (undocumented)
    readonly tags?: readonly string[];
    // (undocumented)
    readonly title?: string;
}

// @public
export function DocumentView(input: {
    open: OpenDocument;
    icon: string | null | undefined;
    note?: string;
    onBack: () => void;
    backLabel: string;
    fixed?: boolean;
    onArchive: () => void;
    onDelete: () => void;
    justCreated?: boolean;
}): JSX.Element;

// @public (undocumented)
export function DropdownMenu(input: React_2.ComponentProps<typeof DropdownMenu_2.Root>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuCheckboxItem(input: React_2.ComponentProps<typeof DropdownMenu_2.CheckboxItem> & {
    inset?: boolean;
}): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuContent(input: React_2.ComponentProps<typeof DropdownMenu_2.Content>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuGroup(input: React_2.ComponentProps<typeof DropdownMenu_2.Group>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuItem(input: React_2.ComponentProps<typeof DropdownMenu_2.Item> & {
    inset?: boolean;
    variant?: "default" | "destructive";
}): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuLabel(input: React_2.ComponentProps<typeof DropdownMenu_2.Label> & {
    inset?: boolean;
}): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuPortal(input: React_2.ComponentProps<typeof DropdownMenu_2.Portal>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuRadioGroup(input: React_2.ComponentProps<typeof DropdownMenu_2.RadioGroup>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuRadioItem(input: React_2.ComponentProps<typeof DropdownMenu_2.RadioItem> & {
    inset?: boolean;
}): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuSeparator(input: React_2.ComponentProps<typeof DropdownMenu_2.Separator>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuShortcut(input: React_2.ComponentProps<"span">): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuSub(input: React_2.ComponentProps<typeof DropdownMenu_2.Sub>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuSubContent(input: React_2.ComponentProps<typeof DropdownMenu_2.SubContent>): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuSubTrigger(input: React_2.ComponentProps<typeof DropdownMenu_2.SubTrigger> & {
    inset?: boolean;
}): React_2.JSX.Element;

// @public (undocumented)
export function DropdownMenuTrigger(input: React_2.ComponentProps<typeof DropdownMenu_2.Trigger>): React_2.JSX.Element;

// @public (undocumented)
export const EMPTY_TRANSCRIPT: Transcript;

// @public
export interface EntityLink {
    // (undocumented)
    key: string;
    // (undocumented)
    relation: string;
}

// @public
export type Entry =
/** Something a person typed, and whatever they attached to it. */
    {
    readonly id: string;
    readonly at: number;
    readonly voice: "person";
    readonly text: string;
    readonly attachments: readonly string[];
    readonly images: readonly PastedImage[];
}
/** The agent's answer. */
| {
    readonly id: string;
    readonly at: number;
    readonly lastAt: number;
    readonly voice: "agent";
    readonly text: string;
}
/** The agent thinking out loud, which not every agent sends. */
| {
    readonly id: string;
    readonly at: number;
    readonly lastAt: number;
    readonly voice: "thought";
    readonly text: string;
}
/**
* A picture the agent answered with.
*
* Its own block rather than something hung off the message beside it, and for
* the same reason a tool call is: the agent went and made something, and the
* text before it and the text after it are two different things to say. It
* also has to fold identically live and on a replay — an agent sends the
* picture in the same run of chunks either way — and a block that joined the
* open message would put it in a different place depending on how fast the
* chunk before it arrived.
*/
| {
    readonly id: string;
    readonly at: number;
    readonly voice: "picture";
    readonly imageId: string | null;
    readonly mimeType: string;
    readonly bytes: number;
}
/** A tool the agent ran. */
| {
    readonly id: string;
    readonly at: number;
    readonly voice: "tool";
    readonly title: string;
    readonly status: string;
    readonly toolCallId: string;
}
/** A plan the agent stated, in the shape it stated it. */
| {
    readonly id: string;
    readonly at: number;
    readonly voice: "plan";
    readonly steps: readonly {
        readonly title: string;
        readonly status: string;
    }[];
}
/** An update this build has no reading for. Shown rather than dropped. */
| {
    readonly id: string;
    readonly at: number;
    readonly voice: "unread";
    readonly update: string | null;
    readonly payload: unknown;
}
/** Something went wrong, in the words it went wrong in. */
| {
    readonly id: string;
    readonly at: number;
    readonly voice: "trouble";
    readonly text: string;
};

// @public
export function explain(failure: unknown): string;

// @public
export interface ExtensionHost {
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly net: ExtensionNet;
    // (undocumented)
    readonly terminal: ExtensionTerminal;
    // (undocumented)
    readonly vault: ExtensionVault;
}

// @public
export interface ExtensionNet {
    fetch(request: NetRequest): Promise<NetResponse>;
}

// @public
export interface ExtensionTerminal {
    close(terminal: string): Promise<void>;
    closeProject(project: string): Promise<void>;
    list(project: string): Promise<readonly TerminalRow[]>;
    open(opening: TerminalOpening): Promise<string>;
    resize(terminal: string, size: TerminalSize): Promise<void>;
    watch(terminal: string, from: number, onEvent: (event: TerminalEvent) => void): Promise<void>;
    write(terminal: string, data: string): Promise<void>;
}

// @public
export interface ExtensionVault {
    forget(name: string): Promise<void>;
    read(name: string): Promise<string>;
    write(name: string, secret: string): Promise<void>;
}

// @public
export interface FieldDeclaration {
    readonly default?: unknown;
    // (undocumented)
    readonly description?: string;
    readonly items?: FieldDeclaration;
    // (undocumented)
    readonly required?: boolean;
    readonly type?: string;
    readonly values?: readonly string[];
}

// @public
export function folderName(path: string): string;

// @public
export function FolderRemovalSheet(input: {
    open: boolean;
    onOpenChange: (open: boolean) => void;
    folder: string | null;
    countRecords: (folder: string) => Promise<number>;
    onDelete: (folder: string) => Promise<void>;
}): JSX.Element;

// @public
export interface Folders {
    readonly byKind: ReadonlyMap<string, readonly MemoryFolder[]>;
    readonly error: string | null;
    // (undocumented)
    readonly isLoading: boolean;
    readonly reload: () => void;
}

// @public
export function FolderSheet(input: {
    open: boolean;
    onOpenChange: (open: boolean) => void;
    parent: string;
    renaming?: string;
    onSubmit: (name: string) => Promise<void>;
}): JSX.Element;

// @public
export function foldersUnder(folders: readonly MemoryFolder[], root: string): readonly MemoryFolder[];

// @public
export function foldTranscript(transcript: Transcript, event: SessionEvent): Transcript;

// @public
export function forgetRememberedConversation(project: string, acpSession: string): Promise<void>;

// @public
export type Freshness = "fresh" | "unverified" | "stale" | "invalid" | (string & {});

// @public
export const FRESHNESS_STATES: readonly ["fresh", "unverified", "stale", "invalid"];

// @public
export function imageFileName(mimeType: string, called?: string): string;

// @public
export interface InstalledExtension {
    // (undocumented)
    readonly id: string;
    readonly integrity?: string;
    readonly prompt?: string;
    readonly source?: string;
    readonly tools?: readonly ToolDeclaration[];
    readonly version: string;
}

// @public
export function isAttachedType(type: MemoryType): boolean;

// @public
export function isVersion(candidate: string): boolean;

// @public
export function KindGlyph(input: {
    icon: string | null | undefined;
    className?: string;
}): JSX.Element;

// @public
export function kindIcon(icon: string | null | undefined): LucideIcon;

// @public
export function KindMark(input: {
    icon: string | null | undefined;
    className?: string;
}): JSX.Element;

// @public
export function Markdown(input: {
    children: string;
    plugins?: readonly MarkdownPlugin[];
}): JSX.Element;

// @public
export type MarkdownBlock = {
    kind: "heading";
    level: 1 | 2 | 3;
    text: string;
} | {
    kind: "picture";
    url: string;
    alt: string;
} | {
    kind: "paragraph";
    text: string;
} | {
    kind: "list";
    ordered: boolean;
    items: string[];
} | {
    kind: "quote";
    text: string;
} | {
    kind: "code";
    text: string;
} | {
    kind: "table";
    header: string[];
    rows: string[][];
} | {
    kind: "rule";
};

// @public
export interface MarkdownPlugin {
    readonly name: string;
    // (undocumented)
    readonly render: (block: MarkdownBlock) => ReactNode | null;
}

// @public
export interface MemoryCounts {
    // (undocumented)
    readonly byFreshness: Readonly<Record<string, number>>;
    // (undocumented)
    readonly byKind: Readonly<Record<string, number>>;
    // (undocumented)
    readonly total: number;
}

// @public
export interface MemoryDocument {
    // (undocumented)
    readonly archived: boolean;
    // (undocumented)
    readonly content: string;
    readonly contentBinary: boolean;
    readonly contentMissing: boolean;
    readonly fields: Readonly<Record<string, unknown>>;
    readonly folder: string | null;
    // (undocumented)
    readonly freshness: Freshness;
    readonly isFolder: boolean;
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly kind: string;
    // (undocumented)
    readonly links: readonly EntityLink[];
    readonly locator: string | null;
    readonly mediaType: string | null;
    readonly observed: readonly string[];
    // (undocumented)
    readonly presence: Presence;
    readonly scope: readonly string[];
    // (undocumented)
    readonly tags: readonly string[];
    // (undocumented)
    readonly title: string;
}

// @public
export interface MemoryFolder {
    readonly describedBy: string | null;
    // (undocumented)
    readonly inRecords: boolean;
    // (undocumented)
    readonly inStorage: boolean;
    readonly path: string;
    readonly records: number;
}

// @public
export function memoryFolders(project: string, folder?: string, subtree?: boolean,
kind?: string): Promise<MemoryFolder[]>;

// @public
export function memoryFolderToll(project: string, folder: string): Promise<number>;

// @public
export interface MemoryRecord {
    // (undocumented)
    readonly archived: boolean;
    readonly fields?: Readonly<Record<string, unknown>>;
    readonly folder: string | null;
    // (undocumented)
    readonly freshness: Freshness;
    readonly isFolder: boolean;
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly kind: string;
    readonly locator: string | null;
    // (undocumented)
    readonly presence: Presence;
    readonly scope: readonly string[];
    // (undocumented)
    readonly tags: readonly string[];
    // (undocumented)
    readonly title: string;
}

// @public
export interface MemorySelection {
    fields?: readonly string[];
    folder?: string;
    folderScope?: "exact" | "subtree";
    // (undocumented)
    freshness?: Freshness[];
    // (undocumented)
    kind?: string;
    // (undocumented)
    limit?: number;
    // (undocumented)
    offset?: number;
}

// @public
export interface MemoryType {
    // (undocumented)
    readonly description: string;
    // (undocumented)
    readonly fieldCount: number;
    readonly fields: Readonly<Record<string, FieldDeclaration>>;
    readonly guidance: string | null;
    readonly icon: string | null;
    readonly kind: string;
    readonly own: boolean;
    readonly relationships: Readonly<Record<string, RelationshipDeclaration>>;
    readonly storage: TypeStorage;
    readonly title: string;
    readonly writable: boolean;
}

// @public
export interface MemoryView {
    // (undocumented)
    readonly counts: MemoryCounts;
    readonly hasMore: boolean;
    // (undocumented)
    readonly records: readonly MemoryRecord[];
    // (undocumented)
    readonly revision: string;
}

// @public
export interface MenuRecordType {
    // (undocumented)
    kind: string;
    // (undocumented)
    title: string;
}

// @public
export function modelOption<T extends {
    category?: string | null;
    type?: string;
}>(options: readonly T[] | null | undefined): T | null;

// @public
export function MoveArea(input: {
    onDrop: (target: unknown, payload: unknown) => void;
    children: ReactNode;
}): JSX.Element;

// @public
export function moveMemoryDocument(project: string, key: string, folder: string): Promise<TransactionResult>;

// @public
export interface NativeEditingCommand {
    // (undocumented)
    predefined: "Cut" | "Copy" | "Paste" | "SelectAll" | "Undo" | "Redo";
}

// @public
export type NativeMenuEntry = NativeMenuItem | NativeEditingCommand | "separator";

// @public
export interface NativeMenuItem {
    // (undocumented)
    enabled?: boolean;
    // (undocumented)
    label: string;
    // (undocumented)
    onSelect?: () => void;
}

// @public
export type NetMethod = "GET" | "HEAD" | "POST" | "PUT" | "PATCH" | "DELETE";

// @public
export interface NetPart {
    // (undocumented)
    readonly base64?: string;
    readonly contentType?: string;
    // (undocumented)
    readonly filename?: string;
    readonly name: string;
    // (undocumented)
    readonly text?: string;
}

// @public
export interface NetRequest {
    readonly body?: string;
    readonly bodyBase64?: string;
    readonly form?: readonly NetPart[];
    readonly headers?: Readonly<Record<string, string>>;
    readonly method?: NetMethod;
    // (undocumented)
    readonly url: string;
}

// @public
export interface NetResponse {
    // (undocumented)
    readonly body: string;
    readonly headers: Readonly<Record<string, string>>;
    readonly ok: boolean;
    // (undocumented)
    readonly status: number;
    readonly url: string;
}

// @public
export interface OpenDocument {
    // (undocumented)
    readonly document: MemoryDocument | null;
    readonly draft: DocumentDraft | null;
    readonly edit: (patch: DocumentPatch) => void;
    readonly editBody: (read: () => string) => void;
    readonly error: string | null;
    readonly forget: (key: string) => void;
    // (undocumented)
    readonly isLoading: boolean;
    readonly reconcile: () => Promise<void>;
    // (undocumented)
    readonly save: SaveState;
    readonly write: () => Promise<void>;
}

// @public
export interface OpenedSession {
    // (undocumented)
    readonly agentName: string;
    // (undocumented)
    readonly agentVersion: string | null;
    // (undocumented)
    readonly configuration: readonly SessionConfigOption[] | null;
    // (undocumented)
    readonly key: string;
    readonly modes: SessionModeState | null;
}

// @public
export interface OpenProject extends ProjectSettings {
    readonly path: string;
}

// @public
export interface OpenQuestion {
    // (undocumented)
    readonly at: number;
    // (undocumented)
    readonly request: PermissionRequest;
    // (undocumented)
    readonly requestId: number;
    // (undocumented)
    readonly toolName: string | null;
}

// @public
export const PAGE_LIMIT = 200;

// @public (undocumented)
export function PanelBody(input: {
    className?: string;
    children: ReactNode;
}): JSX.Element;

// @public
export function PanelFooter(input: {
    children: ReactNode;
}): JSX.Element;

// @public (undocumented)
export function PanelHeader(input: {
    title: string;
    children?: ReactNode;
}): JSX.Element;

// @public
export function PanelPlaceholder(input: {
    headline: string;
    detail?: string;
}): JSX.Element;

// @public
export function PanelSurface(input: {
    className?: string;
    children: ReactNode;
}): JSX.Element;

// @public
export function parentFolder(path: string): string;

// @public
export interface PastedContent {
    // (undocumented)
    readonly data: string;
    // (undocumented)
    readonly mimeType: string;
    // (undocumented)
    readonly name: string;
}

// @public
export interface PastedImage {
    // (undocumented)
    readonly bytes: number;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly mimeType: string;
    // (undocumented)
    readonly name: string;
}

// @public
export const PAUSE_MS = 1500;

// @public
export interface PermissionRequest {
    // (undocumented)
    readonly options: readonly {
        readonly optionId: string;
        readonly name: string;
        readonly kind: string;
    }[];
    // (undocumented)
    readonly toolCall?: {
        readonly title?: string;
        readonly kind?: string;
        readonly locations?: readonly {
            readonly path: string;
        }[];
        readonly rawInput?: unknown;
        readonly _meta?: {
            readonly message?: string;
        };
    };
}

// @public
export type Presence = "present" | "not_on_branch" | "removed" | (string & {});

// @public
export const PROJECT_KEY = "project";

// @public
export const PROJECT_LANGUAGES: readonly [{
    readonly id: "en";
    readonly label: "English";
}, {
    readonly id: "de";
    readonly label: "Deutsch";
}, {
    readonly id: "fr";
    readonly label: "Français";
}, {
    readonly id: "es";
    readonly label: "Español";
}, {
    readonly id: "pt";
    readonly label: "Português";
}, {
    readonly id: "zh";
    readonly label: "中文";
}, {
    readonly id: "ja";
    readonly label: "日本語";
}];

// @public (undocumented)
export type ProjectLanguageId = (typeof PROJECT_LANGUAGES)[number]["id"];

// @public
export function projectRemote(path: string): Promise<string | null>;

// @public
export interface ProjectSettings {
    readonly description: string;
    readonly identifier: string;
    readonly installed: readonly InstalledExtension[];
    // (undocumented)
    readonly language: ProjectLanguageId;
    // (undocumented)
    readonly name: string;
}

// @public
export interface ProjectViewState {
    readonly hidden: readonly string[];
    // (undocumented)
    readonly isHidden: (kind: string) => boolean;
    // (undocumented)
    readonly showAll: () => void;
    // (undocumented)
    readonly toggle: (kind: string) => void;
}

// @public (undocumented)
export function RecordMetadata(input: {
    document: MemoryDocument;
    draft: DocumentDraft;
    type: MemoryType | undefined;
    types: readonly MemoryType[];
    projectPath: string;
    onEdit: (patch: DocumentPatch) => void;
    onWrite: () => void;
}): JSX.Element;

// @public
export function RecordRemovalSheet(input: {
    open: boolean;
    onOpenChange: (open: boolean) => void;
    record: MemoryRecord | null;
    types: readonly MemoryType[];
    dependentsOf: (key: string) => Promise<Dependents>;
    onDelete: (keys: readonly string[]) => Promise<void>;
}): JSX.Element;

// @public
export function refuseIncompatible(required: ApiRequirement): string | null;

// @public
export interface RelationshipDeclaration {
    // (undocumented)
    readonly description?: string;
    // (undocumented)
    readonly target?: string;
}

// @public
export interface RememberedConversation {
    readonly about?: SessionAbout;
    readonly acpSession: string;
    // (undocumented)
    readonly agentId: string;
    // (undocumented)
    readonly agentName: string;
    // (undocumented)
    readonly cwd: string;
    // (undocumented)
    readonly lastSeenMs: number;
    // (undocumented)
    readonly openedAtMs: number;
    readonly parent?: string;
    readonly recordKey?: string;
    readonly source?: SessionSource;
    // (undocumented)
    readonly title: string | null;
    readonly worktree?: Worktree;
}

// @public
export function rememberedConversations(project: string): Promise<RememberedConversation[]>;

// @public
export interface RenameCandidate {
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly locator: string;
    readonly similarity: number;
}

// @public
export function renameMemoryFolder(project: string, from: string, to: string): Promise<TransactionResult>;

// @public
export function renameSession(key: string, title: string): Promise<void>;

// @public
export function resumeSession(project: string, acpSession: string): Promise<OpenedSession>;

// @public
export function saveSessionImage(key: string, id: string, suggestedName: string): Promise<boolean>;

// @public
export type SaveState = {
    readonly status: "clean";
} | {
    readonly status: "edited";
} | {
    readonly status: "saving";
} | {
    readonly status: "saved";
}
/**
* The store refused the write.
*
* `kind` travels with the message because one refusal is answered by doing
* something rather than by trying again: code history that was rewritten
* refuses every write, identically, until it is reconciled — and a view that
* only had the prose could offer nothing but a retry that cannot work.
*/
| {
    readonly status: "failed";
    readonly message: string;
    readonly kind: string | null;
};

// @public
export interface ScanChange {
    readonly candidates?: readonly RenameCandidate[];
    // (undocumented)
    readonly change: "edited" | "moved" | "missing" | "returned" | "new" | "unmatched" | (string & {});
    readonly contentHash?: string;
    // (undocumented)
    readonly from?: string;
    // (undocumented)
    readonly key?: string;
    // (undocumented)
    readonly locator?: string;
    // (undocumented)
    readonly presence?: Presence;
    // (undocumented)
    readonly to?: string;
}

// @public
export interface ScanOutcome {
    readonly applied: number;
    // (undocumented)
    readonly changes: readonly ScanChange[];
    // (undocumented)
    readonly revision: string | null;
    readonly scanned: number;
}

// @public (undocumented)
export function ScrollArea(input: React_2.ComponentProps<typeof ScrollArea_2.Root> & {
    viewportRef?: React_2.Ref<HTMLDivElement>;
}): React_2.JSX.Element;

// @public (undocumented)
export function ScrollBar(input: React_2.ComponentProps<typeof ScrollArea_2.ScrollAreaScrollbar>): React_2.JSX.Element;

// @public
export interface SentImage {
    readonly bytes: number;
    // (undocumented)
    readonly imageId: string | null;
    // (undocumented)
    readonly mimeType: string;
}

// @public
export interface SessionAbout {
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly kind: string;
    // (undocumented)
    readonly title: string;
}

// @public
export function sessionBacklog(key: string): Promise<{
    events: SessionEvent[];
    dropped: number;
}>;

// @public
export interface SessionConfigOption {
    // (undocumented)
    readonly category?: string | null;
    // (undocumented)
    readonly currentValue?: string;
    // (undocumented)
    readonly description?: string | null;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly name: string;
    // (undocumented)
    readonly options?: readonly SessionConfigValue[] | readonly {
        readonly name: string;
        readonly options: readonly SessionConfigValue[];
    }[];
    // (undocumented)
    readonly type?: string;
}

// @public (undocumented)
export interface SessionConfigValue {
    // (undocumented)
    readonly description?: string | null;
    // (undocumented)
    readonly name: string;
    // (undocumented)
    readonly value: string;
}

// @public
export class SessionError extends Error {
    constructor(kind: string, message: string);
    // (undocumented)
    readonly kind: string;
}

// @public
export type SessionEvent = {
    readonly kind: "status";
    readonly seq: number;
    readonly atMs: number;
    readonly status: SessionStatus;
    readonly detail: string | null;
} | {
    readonly kind: "prompt";
    readonly seq: number;
    readonly atMs: number;
    readonly text: string;
    readonly attachments: readonly string[];
    readonly images: readonly PastedImage[];
} | {
    readonly kind: "update";
    readonly seq: number;
    readonly atMs: number;
    readonly update: string | null;
    readonly recognized: boolean;
    readonly payload: Record<string, unknown>;
    readonly replayed?: boolean;
} | {
    readonly kind: "permission";
    readonly seq: number;
    readonly atMs: number;
    readonly requestId: number;
    readonly toolName: string | null;
    readonly request: PermissionRequest;
} | {
    readonly kind: "permissionSettled";
    readonly seq: number;
    readonly atMs: number;
    readonly requestId: number;
    readonly chosen: string | null;
} | {
    readonly kind: "configuration";
    readonly seq: number;
    readonly atMs: number;
    readonly options: readonly SessionConfigOption[];
} | {
    readonly kind: "modes";
    readonly seq: number;
    readonly atMs: number;
    readonly modes: SessionModeState;
};

// @public
export function sessionImage(key: string, id: string): Promise<{
    readonly mimeType: string;
    readonly data: string;
}>;

// @public
export interface SessionMode {
    // (undocumented)
    readonly description?: string | null;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly name: string;
}

// @public
export interface SessionModeState {
    // (undocumented)
    readonly availableModes: readonly SessionMode[];
    // (undocumented)
    readonly currentModeId: string;
}

// @public
export interface SessionRow {
    readonly about?: SessionAbout;
    readonly acceptsImages: boolean;
    readonly acpSession?: string;
    // (undocumented)
    readonly agentId: string;
    // (undocumented)
    readonly agentName: string;
    // (undocumented)
    readonly cwd: string;
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly openedAtMs: number;
    readonly parent?: string;
    readonly project: string;
    readonly source?: SessionSource;
    // (undocumented)
    readonly status: SessionStatus;
    readonly title: string | null;
    readonly worktree?: Worktree;
}

// @public
export interface SessionSource {
    readonly about?: string;
    readonly extensionId: string;
    readonly extensionName: string;
    readonly handler: string;
    readonly work: string;
}

// @public
export type SessionStatus =
/** The process is being raised and the session opened. */
"starting"
/** Open, and not in a turn. */
| "ready"
/** A turn is running. */
| "working"
/**
* A turn has been said into it and is waiting its turn to run. What a
* conversation delegated from one that already has a delegated run under it
* says until that one is finished.
*/
| "queued"
/** Stopped on a question only a person can answer. */
| "asking"
/** Ended by itself, or its process died. */
| "ended"
/** Could not be raised, or fell over. */
| "failed";

// @public
export function Sheet(input: React_2.ComponentProps<typeof Dialog.Root>): React_2.JSX.Element;

// @public (undocumented)
export function SheetContent(input: React_2.ComponentProps<typeof Dialog.Content>): React_2.JSX.Element;

// @public (undocumented)
export function SheetDescription(input: React_2.ComponentProps<typeof Dialog.Description>): React_2.JSX.Element;

// @public
export function SheetFooter(input: React_2.ComponentProps<"footer">): React_2.JSX.Element;

// @public
export function SheetHeader(input: React_2.ComponentProps<"header">): React_2.JSX.Element;

// @public (undocumented)
export function SheetTitle(input: React_2.ComponentProps<typeof Dialog.Title>): React_2.JSX.Element;

// @public
export function showNativeContextMenu(event: {
    preventDefault: () => void;
}, entries: readonly NativeMenuEntry[]): boolean;

// @public (undocumented)
export function SourceList(input: {
    label: string;
    items: readonly SourceListItem[];
    activeId: string;
    rail?: boolean;
    onSelect: (id: string) => void;
    onReorder?: (ids: readonly string[]) => void;
}): JSX.Element;

// @public
export interface SourceListItem {
    readonly badge?: {
        readonly kind: "count";
        readonly value: number;
    } | {
        readonly kind: "dot";
    };
    // (undocumented)
    readonly icon: LucideIcon;
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly label: string;
    readonly menu?: () => readonly NativeMenuEntry[];
    readonly note?: string;
}

// @public (undocumented)
export function SourceTree(input: {
    label: string;
    items: ReadonlyMap<string, SourceTreeItem>;
    rootId: string;
    activeId: string | null;
    expanded: readonly string[];
    onSelect: (id: string) => void;
    onExpandedChange: (expanded: readonly string[]) => void;
    indent?: number;
}): JSX.Element;

// @public
export interface SourceTreeItem {
    readonly children?: readonly string[];
    readonly count?: number;
    readonly drag?: unknown;
    readonly drop?: unknown;
    readonly emphasised?: boolean;
    // (undocumented)
    readonly icon?: LucideIcon;
    readonly id: string;
    // (undocumented)
    readonly label: string;
    readonly menu?: () => readonly NativeMenuEntry[];
    readonly muted?: boolean;
    readonly tooltip?: ReactNode;
}

// @public
export function startSession(args: {
    agentId: string;
    cwd: string;
    model?: string | null;
    about?: SessionAbout | null;
    worktree?: WorktreeChoice | null;
    parent?: string | null;
}): Promise<OpenedSession>;

// @public
export function StateMark(input: {
    freshness: Freshness;
    className?: string;
}): JSX.Element;

// @public
export function stopSession(key: string): Promise<void>;

// @public
export function supportsApiRange(range: string): boolean;

// @public
export const SYNC_API_VERSION: "3.9.0";

// @public
export const SYNC_CAPABILITIES: readonly ["records", "agents.acp", "markdown.plugins", "native-menu", "folders", "sheets", "net", "net.write", "vault", "background", "schedule", "work.agent", "agent.tools", "terminal"];

// @public (undocumented)
export type SyncCapability = (typeof SYNC_CAPABILITIES)[number];

// @public
export interface TableCommands {
    // (undocumented)
    deleteColumn: () => void;
    // (undocumented)
    deleteRow: () => void;
    // (undocumented)
    deleteTable: () => void;
    // (undocumented)
    insertColumnAfter: () => void;
    // (undocumented)
    insertColumnBefore: () => void;
    // (undocumented)
    insertRowAbove: () => void;
    // (undocumented)
    insertRowBelow: () => void;
}

// @public (undocumented)
export const TableCommandsProvider: Provider<(commands: TableCommands | null) => void>;

// @public
export type TerminalEvent = {
    readonly kind: "output";
    readonly from: number;
    readonly to: number;
    readonly gapped: boolean;
    readonly base64: string;
} | {
    readonly kind: "ended";
    readonly code: number;
    readonly signal: string | null;
} | {
    readonly kind: "gone";
};

// @public
export interface TerminalOpening {
    readonly cwd: string;
    readonly project: string;
    // (undocumented)
    readonly size: TerminalSize;
}

// @public
export interface TerminalRow {
    readonly exit?: {
        readonly code: number;
        readonly signal: string | null;
    };
    // (undocumented)
    readonly id: string;
    // (undocumented)
    readonly owner: string;
}

// @public
export interface TerminalSize {
    // (undocumented)
    readonly cols: number;
    // (undocumented)
    readonly rows: number;
}

// @public
export interface ToolDeclaration {
    readonly description: string;
    readonly input?: unknown;
    readonly name: string;
}

// @public (undocumented)
export function Tooltip(input: React_2.ComponentProps<typeof Tooltip_2.Root>): React_2.JSX.Element;

// @public (undocumented)
export function TooltipContent(input: React_2.ComponentProps<typeof Tooltip_2.Content>): React_2.JSX.Element;

// @public (undocumented)
export function TooltipProvider(input: React_2.ComponentProps<typeof Tooltip_2.Provider>): React_2.JSX.Element;

// @public (undocumented)
export function TooltipTrigger(input: React_2.ComponentProps<typeof Tooltip_2.Trigger>): React_2.JSX.Element;

// @public
export interface TransactionResult {
    // (undocumented)
    changed_keys: string[];
    // (undocumented)
    revision: string;
}

// @public
export interface Transcript {
    readonly detail: string | null;
    readonly dropped: number;
    // (undocumented)
    readonly entries: readonly Entry[];
    readonly mode: string | null;
    readonly question: OpenQuestion | null;
    // (undocumented)
    readonly status: SessionStatus;
    readonly stopReason: string | null;
    readonly usage: Usage | null;
}

// @public
export interface TypeDefinition {
    // (undocumented)
    description: string;
    // (undocumented)
    icon: string;
    // (undocumented)
    kind: string;
    storage?: TypeStorage;
    // (undocumented)
    title: string;
}

// @public
export function TypeFilter(input: {
    types: readonly MemoryType[];
    counts?: Readonly<Record<string, number>>;
    view: ProjectViewState;
    verb?: string;
    align?: "start" | "end";
}): JSX.Element | null;

// @public
export function typeName(types: readonly MemoryType[], kind: string): string;

// @public
export function typeOfLocator(types: readonly MemoryType[], locator: string): MemoryType | null;

// @public
export function TypeRemovalSheet(input: {
    open: boolean;
    onOpenChange: (open: boolean) => void;
    type: MemoryType | null;
    countRecords: (kind: string) => Promise<number>;
    onDelete: (kind: string) => Promise<number>;
}): JSX.Element;

// @public
export function TypeSheet(input: {
    open: boolean;
    onOpenChange: (open: boolean) => void;
    editing: MemoryType | null;
    onSubmit: (type: TypeDefinition) => Promise<void>;
    existing: readonly MemoryType[];
    projectPath: string;
}): JSX.Element;

// @public
export interface TypeStorage {
    readonly folder?: string | null;
}

// @public
export function UnmatchedFiles(input: {
    files: readonly ScanChange[];
    types: readonly MemoryType[];
    onResolve: (file: ScanChange, kind: string, adopt?: string) => Promise<void>;
}): JSX.Element | null;

// @public
export function updateMemoryDocument(project: string, key: string, edits: DocumentPatch): Promise<MemoryDocument | null>;

// @public
export interface Usage {
    // (undocumented)
    readonly cachedReadTokens?: number | null;
    // (undocumented)
    readonly cachedWriteTokens?: number | null;
    // (undocumented)
    readonly inputTokens?: number;
    // (undocumented)
    readonly outputTokens?: number;
    // (undocumented)
    readonly thoughtTokens?: number | null;
    // (undocumented)
    readonly totalTokens?: number;
}

// @public
export function usageLines(usage: Usage | null): readonly {
    label: string;
    value: number;
}[];

// @public
export function useAgents(): {
    readonly agents: readonly Agent[];
    readonly isLoading: boolean;
    readonly reload: () => void;
};

// @public (undocumented)
export function useAgentSession(key: string | null): AgentSession;

// @public (undocumented)
export function useAppMenu(file: WindowCommands | null, enabled?: boolean): void;

// @public
export function useBadge(report: BadgeReport): void;

// @public (undocumented)
export function useCorpus(projectPath: string, selection?: MemorySelection, hidden?: readonly string[], active?: boolean): Corpus;

// @public (undocumented)
export function useDocument(projectPath: string, key: string | null): OpenDocument;

// @public
export function useDragHandle(id: string, payload: unknown): {
    "data-dragging": boolean;
    role: string;
    tabIndex: number;
    'aria-disabled': boolean;
    'aria-pressed': boolean | undefined;
    'aria-roledescription': string;
    'aria-describedby': string;
    ref: (element: HTMLElement | null) => void;
};

// @public (undocumented)
export function useFolders(projectPath: string, kinds: readonly string[], revision: string | null, active?: boolean): Folders;

// @public
export function useLiveSessions(active?: boolean): {
    readonly sessions: readonly SessionRow[];
    readonly reload: () => void;
};

// @public
export function useOpenRecord(): ((record: {
    key: string;
    kind: string;
}) => void) | null;

// @public (undocumented)
export function useProjectView(projectPath: string): ProjectViewState;

// @public
export interface WindowCommands {
    createRecord: ((kind: string) => void) | null;
    createType: (() => void) | null;
    selected: MenuRecordType | null;
    table: TableCommands | null;
}

// @public
export function withDropped(transcript: Transcript, dropped: number): Transcript;

// @public
export interface Worktree {
    readonly base?: string;
    readonly baseCommit: string;
    readonly head: string;
    readonly path: string;
}

// @public
export type WorktreeChoice = "new" | {
    readonly path: string;
};

// @public
export class WorktreeError extends Error {
    constructor(kind: string, message: string);
    // (undocumented)
    readonly kind: string;
}

// @public
export function worktreesIn(project: string): Promise<readonly Worktree[]>;

// (No @packageDocumentation comment for this package)

```
